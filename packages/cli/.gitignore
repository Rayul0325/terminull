# Pack-time artifacts (contract §D1) — generated by prepack, never committed.
# npm pack ships these via `files`; the workspace stays git-clean (gate f).
# No trailing slash so `git check-ignore` matches even before prepack creates them.
dist-pack
web-dist
scripts-pack
*.tgz

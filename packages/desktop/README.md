# @terminull/desktop

Placeholder workspace slot for the Terminull desktop shell.

The Electron-based desktop app lands in **M10**. This package currently has no
Electron dependency, no build step, and no tests — it exists so the workspace
boundary and the `@terminull/desktop` package name are reserved ahead of time.

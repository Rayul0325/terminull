{
  "name": "@terminull/session-host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "paneld": "./dist/bin.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "//": "node-pty is a real runtime dependency (native). Its darwin prebuild ships a non-executable spawn-helper, so pnpm-workspace.yaml lists node-pty under onlyBuiltDependencies; a fresh install may still need a from-source rebuild (npm_config_build_from_source=true) — see the M2 progress notes.",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@terminull/shared": "workspace:*",
    "node-pty": "^1.0.0"
  }
}
